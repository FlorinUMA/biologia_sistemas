#!/usr/bin/env Rscript

# igraph
if (!require("igraph")) {
    install.packages("igraph")
}

# rmarkdown
if (!require("sets")) {
  install.packages("sets")
}

# enrichR
if (!require("enrichR")) {
    install.packages("enrichR")
}
